# Тест «видали 40%» — AGENTS.md

- Рядків до тесту: 35; видалено: ~14 (~40%)
- Що видалено: https://github.com/adamogorodnik/AI-Lab1/commit/8031b83
- Контрольна задача (дослівно): «Add mile-to-km and km-to-mile conversion functions in src/, with tests. Report when done.»
- Інструмент, модель, режим дозволів: GitHub Copilot CLI, default mode
- Журнал, повний файл: сесія 9c098f07 (гілка lab1/agents-md-40-full, коміт b1fc8a3)
- Журнал, урізаний файл: сесія f2038533 (гілка lab1/agents-md-40-cut, коміт 8031b83)

| Видалений рядок | Що змінилося в поведінці | Доказ (рядок журналу) | Рішення |
|---|---|---|---|
| Stack lines (TS/Next/Vitest, DB, CSS) | Нічого — агент бачив package.json напряму | однакові view/glob виклики в обох сесіях | видалити назавжди |
| `npm run typecheck`, `npm run lint` у Commands | Нічого — агент все одно запустив typecheck і lint самостійно | `bash: npm run typecheck`, `bash: npm run lint` в обох сесіях | видалити назавжди |
| `.env` / network / package-lock / migrations / fix-test правила в Межі | Не перевірено — задача не торкалась цих сфер | немає відповідних викликів у жодній сесії | тест непереконливий, лишити правила (ризик високий, ціна низька) |
| dependencies / DB-editing approval | Не перевірено — задача не додавала залежностей і не працювала з БД | немає відповідних викликів | лишити правила |
| Model pricing / commit format у Домовленості | Нічого — задача не стосувалась вибору моделі чи комітів | — | pricing-правило лишити (стосується іншої задачі), commit format — під питанням |

Підсумок: тест показав, що правила стеку і дублювання команд typecheck/lint — справжній баласт для будь-якої задачі (модель і так їх запускає). Критичні за змістом Boundaries-правила (.env, package-lock, migrations) тест не активував, бо контрольна задача занадто вузька — вони лишаються в файлі за принципом "низька ціна, високий ризик", а не тому що тест довів їх необхідність.