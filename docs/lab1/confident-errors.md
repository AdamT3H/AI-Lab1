# Впевнені помилки — Лабораторна 1

## Плани: задача /api/health

| Інструмент | Режим | Файли, які збирався чіпати | Чим збирався довести | Збігається з контрактом? |
|---|---|---|---|---|
| Copilot CLI | plan | app/api/health/route.ts | npm run typecheck, npm test, npm run lint, npm run build | так — явно вказав "не змінювати src/health.ts та tests/health.test.ts" |
| Codex | read-only sandbox / plan | app/api/health/route.ts | git diff, перевірка вручну | так — прочитав контракт перед реалізацією, не чіпав тест/схему |

## Помилки

| # | Інструмент | Режим | Що запропоновано / заявлено | Що насправді (executed) | Чому це помилка | Як спіймали | Рядок журналу |
|---|---|---|---|---|---|---|---|
| 1 | Copilot CLI | build | Журнал мав фіксувати шлях зміненого файлу через apply_patch | `input.files` для apply_patch-подій завжди порожній масив `[]` | Дефект налаштування журналу — неможливо простежити за самим журналом, який файл змінено; доводиться перевіряти через git diff | grep -i "health" на журналі Copilot не знайшов жодного рядка, хоча зміни точно були | .agent-log/copilot.jsonl, сесія 08d84794 |
| 2 | Codex | plan → default | Мав переконатись, що тест червоний до реалізації (контракт вимагає маршруту, якого ще нема) | npm test викликано лише один раз — після apply_patch (23:20:19 патч, 23:20:28 тест), жодного запуску тесту до зміни коду | Агент не підтвердив емпірично початковий стан, поклався на читання коду, а не на факт запуску | Порівняв часові мітки apply_patch і npm test у журналі | .agent-log/codex.jsonl, сесія 01a0ac84 |
| 3 | Copilot CLI | plan → build | Очікувані виклики для цієї задачі: view, apply_patch, bash (typecheck/test/lint/build) | Двічі викликано інструмент "sql" з порожнім input (23:11:40 і 23:14:26) — задача /api/health не працює з жодною БД | Нерелевантний виклик інструмента без пояснення в input; або дефект журналювання (hook неправильно підписав внутрішню подію), або агент невиправдано торкнувся чогось пов'язаного з БД | Знайдено при перегляді повного журналу сесії реалізації (рядки між exit_plan_mode і фінальним apply_patch) | .agent-log/copilot.jsonl, сесія 08d84794 |